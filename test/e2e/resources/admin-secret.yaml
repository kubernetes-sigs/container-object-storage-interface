apiVersion: v1
kind: Secret
metadata:
  name: cosi-sample-s3-admin-secret
  namespace: ($driverNamespace)
stringData:
  AWS_ENDPOINT_URL: ($s3Endpoint)
  AWS_REGION: ($s3Region)
  AWS_ACCESS_KEY_ID: ($adminAccessKeyId)
  AWS_SECRET_ACCESS_KEY: ($adminSecretAccessKey)
